//Acess spacifiers---
// private --with in class only 
//public -- can be used in the class and outside 
//protected--with in class and Inherited class;
//scope resolution operator :: (Counter::Counter Operator++()){
                                // count++}`
